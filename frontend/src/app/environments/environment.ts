export const environment = {
  production: false,
  // Base URL for the backend API.  When using the development server the
  // proxy configuration (proxy.conf.json) rewrites /api to http://localhost:5000.
  apiUrl: ''
};